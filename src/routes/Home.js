import ListBox from "../components/ListBox";
import { Wrapper, Title } from "../styles/routes/StyledHome";
const Home = () => {
  
  return (
    <Wrapper>
      <Title>Title</Title>
      <ListBox />
    </Wrapper>
  );
};

export default Home;
