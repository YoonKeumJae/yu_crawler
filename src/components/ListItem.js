import styled from "styled-components";

const Wrapper = styled.div``;

const ListItem = () => {
  return (
    <Wrapper>
      <span>item1, title, date, url, from, addBookmark</span>
    </Wrapper>
  );
};

export default ListItem;
